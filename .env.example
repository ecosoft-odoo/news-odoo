# ============================================
# Odoo Daily News — secrets (gitignored)
# ============================================

# Required: Groq API key (https://console.groq.com/keys)
GROQ_API_KEY=gsk_replace_me

# Required: Discord incoming webhook URL
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/replace_me

# Optional: GitHub PAT เพิ่ม rate limit 60 → 5000 req/hr
GITHUB_TOKEN=ghp_replace_me

# Optional: web app secret key (เปลี่ยนใน production)
FLASK_SECRET_KEY=change-me-in-production

# Optional: web app port (default 5000)
PORT=5000
